{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run GuavAI File",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/src/main.py",
            "args": ["${file}"],
            "console": "integratedTerminal"
        }
    ]
}
